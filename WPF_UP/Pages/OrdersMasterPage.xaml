<Page x:Class="WPF_UP.Pages.OrdersMasterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WPF_UP.Pages"
      xmlns:collections="clr-namespace:System.Collections;assembly=System.Runtime"
      mc:Ignorable="d"
      Title="OrdersMasterPage" Height="720" Width="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="0">
            <DataGrid Name="DataGrib" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" AutoGenerateColumns="True" Background="#363636" SelectionMode="Single">
            </DataGrid>
        </Grid>
        <Button Grid.Row="1" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Click="ButtonBase_OnClick">
            <TextBlock Text="Выполнить"></TextBlock>
        </Button>
    </Grid>
</Page>
